<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <meta name="channel" content="<%= "frankt:#{@user_id}" %>">

    <title>Hello TestApplication!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">

        <nav>
          <%= link "Home", to: page_path(@conn, :index) %>
          <%= link "Greeting", to: greet_path(@conn, :index) %>
          <%= link "Dynamic Form", to: form_path(@conn, :index) %>
          <%= link "Filtering a list", to: filter_path(@conn, :index) %>
          <%= link "Chat", to: chat_path(@conn, :index) %>
        </nav>

        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
